import { StyleSheet } from "react-native";

const stylesFn = () => {
  return StyleSheet.create({
    title: {
      fontSize: 25,
      fontWeight: "bold",
      alignSelf: "center",
      textAlign: "center"
    },
  });
};

export default stylesFn;
