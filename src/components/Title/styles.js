import { StyleSheet } from "react-native";

const stylesFn = () => {
  return StyleSheet.create({
    title: {
      fontSize: 25,
      fontWeight: "bold",
      alignSelf: "center",
      textAlign: "center",
      marginTop: 5,
      marginBottom: 5,
      color: "#191A19",
    },
  });
};

export default stylesFn;
